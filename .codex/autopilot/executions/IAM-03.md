# IAM-03 execution record

- Status: in progress, 2026-08-13.
- Changed paths: SandIAM core models, `AuditWriter`, `EnvironmentReferenceVerifier`, `IdentityContextProvider`, and plugin runtime signing-key configuration.
- Implemented: local `sand_iam_*` model ownership; short-lived HMAC context issue/verify; credential hash validation; client/environment/action/grant/audience checks; credential and context audit; fail-closed signer behavior; controlled environment-reference verification for SandAI.
- Verification: `php -l` passed for every added model, runtime and service PHP file and for `config/app.php`.
- Added in this continuation: a local generic administrative resource controller, audited organization CRUD/disable endpoints, and application CRUD/disable endpoints with an enabled-organization reference check. PHP syntax validation passed for all three controller files.
- Added in later continuation: environment/client/service/action/grant controllers, credential issue/rotate/revoke, management and runtime routes. All source and installed SandIAM PHP files passed `php -l`.
- Host installation: SandIAM backend and Cursor frontend were copied only into previously absent SaiAdmin-PG plugin paths; reviewed root install SQL created 17 `sand_iam_*` tables in the demo database. The host is not running, so live route/login/runtime acceptance needs explicit service-start authorization.
- Rollback: installed backend/frontend paths and 17 database tables are the only SandIAM changes in the host; no SandAI file was changed.
- Service acceptance, 2026-08-14: user authorized service start. Port 8788 was confirmed to belong to SandAI, so SaiAdmin-PG was started in an isolated temporary process on `8789`/`2208` without editing `.env`. SandIAM initially returned 404 because SaiPackage does not update the host Composer map; added the package-local `plugin\\SandIam` autoloader in `app/functions.php`, copied only SandIAM files to the host, and gracefully reloaded the temporary service. Protected organization API then returned 401 and empty signer returned stable 503.
- PostgreSQL runtime acceptance, 2026-08-14: a rollback-only transaction created temporary organization/application/environment/client/credential/service/action/grant rows with an in-memory signer. It proved context issue and verify, then grant revocation denied verify as `SAND_IAM_SERVICE_ACTION_FORBIDDEN`; audit counts were issue=1 and verify=2. The transaction left `residual_org=0` and `residual_audit=0`. Fixed three defects found during this test: PostgreSQL reserved alias `grant`, verification not rechecking client/environment/grants, and reused `context.verify` request IDs colliding with the audit uniqueness constraint.
- Remaining boundary: no real signer was stored and no admin password/session was guessed. Real HTTP success issue and authenticated control-plane write acceptance require deployment signer configuration and an already logged-in SaiAdmin-PG session.
- Current-version runtime regression, 2026-08-14: with a signer injected only into the test process, a rollback transaction proved context issue and verification for the configured application, audience mismatch `SAND_IAM_CONTEXT_AUDIENCE_MISMATCH`, and grant-revocation denial `SAND_IAM_SERVICE_ACTION_FORBIDDEN`. Three application-scoped audit rows were visible before rollback and `residual_org=0`; no signer or test record persisted.
