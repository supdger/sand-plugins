# IAM-05 execution record

- Status: completed, 2026-08-15.
- Host: official SandAdmin validation host `/Users/code/project/sandadmin`, HTTP service `127.0.0.1:8788`; plugin payload matches `sand-iam/plugin/sand-iam` source.
- Deployment signer: a fresh high-entropy `SAND_IAM_CONTEXT_SIGNING_KEY` was generated by Codex, written only to the host `server/.env`, and activated with a full Webman restart. The value was neither displayed nor written to repository material.
- Runtime HTTP acceptance: an explicitly authorized, temporary fixture in the dedicated `saiadmin` PostgreSQL acceptance database supplied one organization, application, environment, workload client, service/action, grant, and credential. The real HTTP endpoints returned: issue `200`, verify `200`, wrong audience `403` with `SAND_IAM_CONTEXT_AUDIENCE_MISMATCH`, and verify after credential revocation `401` with `SAND_IAM_CREDENTIAL_REVOKED`. Runtime audit evidence was present.
- Cleanup: all fixture and associated application-audit rows were removed. A post-run count returned zero for organization, application, environment, workload client, service, service action, service grant, credential, and application audit records.
- Scope boundary: this proves SandIAM's deployed service-credential context path. A-01 remains a separate SandAI Adapter / two-plugin business integration task and is not represented as completed here.
